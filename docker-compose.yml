services:
  squid-bot:
    build:
      context: .
    container_name: squid-bot
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      ADMIN_ID: ${ADMIN_ID}
    networks:
      - default
    volumes:
      - ${SQUID_DIR}:/app/squid
      - ./db:/app/db
